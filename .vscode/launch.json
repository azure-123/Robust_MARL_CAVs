{
    "version": "0.2.0",
    "configurations": [
      {
        "name": "Python Debug",
        "type": "python",
        "request": "launch",
        "program": "${file}",
        // "args": ["--env-config=highway", "--config=qmix"],
        "args": ["--option=test"],
        "cwd": "${workspaceFolder}",
        "console": "integratedTerminal",
        "purpose": ["debug-in-terminal"],
        "justMyCode": false
      }
    ]
  }